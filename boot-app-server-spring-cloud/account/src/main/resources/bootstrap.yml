
spring:
  application:
    name: account
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: dev
      name: common,account

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/

server:
  port: 8200
