
spring:
  application:
    name: gateway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: dev
      name: common,gateway

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/

server:
  port: 80
